apiVersion: kudo.dev/v1beta1
kind: TestStep
commands:
  - command: kubectl -n default create serviceaccount kuttlaccount -o yaml
  - command: kubectl create clusterrolebinding --clusterrole=cluster-admin --serviceaccount=default:kuttlaccount kuttlcrb
